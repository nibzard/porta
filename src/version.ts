/**
 * Implementation version of the Portable library.
 *
 * Keep this value in sync with the `version` field in `package.json`.
 * A test enforces the match.
 */
export const VERSION = "0.1.0";

/**
 * The specification version this library implements.
 *
 * Conformance reports carry this value, so a report always names the
 * specification its cases were written against (SPEC.md section 21).
 */
export const SPEC_VERSION = "0.1.0-draft.1";
